name: build and parallel test
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello > tim
      - run: ls
  test:
    runs-on: ubuntu-latest
    environment: production
    needs: build
    steps:
      - run: ls
      # - run: exit 1
