name: matrix 3
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node_index: [0, 1, 2]
        total_nodes: [3]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Test ${{ matrix.repo }}
        run: |
          echo ci node index is $CI_NODE_INDEX 
          echo ci node index is $CI_NODE_INDEX > $CI_NODE_INDEX
          ls
        env:
          CI_NODE_INDEX: ${{ matrix.node_index }}
          CI_NODE_TOTAL: ${{ matrix.total_nodes }}
      - name: list
        run: ls
