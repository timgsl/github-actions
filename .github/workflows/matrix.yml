name: matrix includes
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: [api, common-services]
        command: [test --fail-fast --color]
        include:
          - repo: frontend
            command: [test]
          - repo: common-lib
            command: [test --fail-fast --color]
    steps:
      - name: Test ${{ matrix.repo }}
        # run: yarn workspace @rc-main/${{ matrix.repo }} ${{ toJSON(matrix.command) }}
        run: |
          echo repo ${{ matrix.repo}}
          echo command ${{ matrix.command}}
